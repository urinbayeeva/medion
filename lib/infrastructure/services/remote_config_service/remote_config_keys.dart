class RemoteConfigKeys {
  const RemoteConfigKeys();

  static const bannerIsAvailable = "banner_is_available";
  static const poa = 'poa';

  static const keys = {
    bannerIsAvailable,
    poa,
  };
}
